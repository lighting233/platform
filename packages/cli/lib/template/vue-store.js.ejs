import { defineStore } from 'pinia';
const <%= 'use' + data.name.charAt(0).toUpperCase() + data.name.slice(1) + 'Store' %> = defineStore('<%= data.name %>', {
  state: => ({
    
  }),
  actions: {
    async fetchSomethingAction() {
      
    }
  }
})

export default <%= 'use' + data.name.charAt(0).toUpperCase() + data.name.slice(1) + 'Store' %>
